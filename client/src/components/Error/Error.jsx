import React, { useState } from "react";

const Error = () => {
  return (
    <div
      style={{
        textAlign: "center",
        color: "red",
        cursor: "pointer",
      }}
    >
      Error
    </div>
  );
};

export default Error;
